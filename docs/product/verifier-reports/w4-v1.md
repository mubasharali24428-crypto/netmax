# W4 Verifier Report — V1 (X6: BloatStory model + view)

**Mission:** W4 · **Vertical:** X6 story copy vs engine rubric · **Date:** 2026-08-23
**Target lanes:** TEAM-1 T1-a `desktop/SwiftNetMax/Sources/netmax-desktop/BloatStory.swift`, T1-b `BloatStoryView.swift`
**Baseline:** branch `main` @ e65045d

## VERDICT: READY-TO-MERGE

No blockers. Two minor notes below, neither merge-gating.

## Ground truth & method

Engine rubric extracted from source (`netmax.py:270-272`, applied at :307-309):
`(5,"A+") (30,"A") (60,"B") (200,"C") (400,"D")`, comparison strict `delta_ms < limit`, fall-through `"F"`.

Evidence collected without reading any builder self-report. All commands run by the verifier;
outputs pasted/saved below are real execution results, not builder claims.

## Evidence

### 1. Footprint audit — CLEAN
`git status --porcelain`: vertical touches only its two owned untracked files. Sibling dirty
files (`desktop/README.md`, Anomaly*, `scripts/*`) belong to other lanes. Zero writes outside
the verifier-owned report path. No commits made (repo law honored).

### 2. Rubric parity — PASS (static, machine-diffed)
Ladders parsed programmatically from BOTH sources (no transcription): engine
`[(5,A+) (30,A) (60,B) (200,C) (400,D)] else F` vs Swift `case ..<5/<30/<60/<200/<400 … default F`
— structurally identical; Swift `..<` exclusivity ≡ engine strict `<`.
23 simulated deltas (all 10 mandated boundaries + neighbors ±0.01, 0, negative, 1000):
**engine == swift on every probe.**

### 3. Rubric parity — PASS (runtime, shipped code executed)
Compiled the actual `BloatStory.swift` standalone (`swiftc -DDEBUG`) with a verifier harness
(`/tmp/v1_harness_main.swift` → `/tmp/v1_harness`). Real output:

```
4.9→A+  5.0→A  29→A  30→B  59→B  60→C  199→C  200→D  399→D  400→F   (10/10 OK)
make(fromGrade:) A+/A/B/C/D/F round-trip OK · unknown "E" correctly nil
BloatStorySelfCheck.verify() failures reported: 0     ← shipped DEBUG guard ran live
copy completeness: all bands 3 activities, cause+suggestion non-empty
HARNESS RESULT: ALL GREEN (exit 0)
```

Shipped `boundaryCases` table cross-checked against engine: 12/12 entries correct.

### 4. Build check — PASS
`swift build` exit 0 (Swift 6.3.3, tools-mode 5.9, arm64). Binary proven to contain this
vertical's code: `strings netmax-desktop | grep` matched shipped copy verbatim ("Bufferbloat
story", "Latency under load", "What you can do", full F-band suggestion).

### 5. Copy law — PASS
90 string literals scanned (68 + 22, comments stripped to avoid false hits): **zero**
marketing-speak, **zero** NetMax-fix promises. The only suggestion mentioning NetMax is
measurement-scoped: "NetMax can measure the result — the queueing itself is configured on the
router." D/F suggestions echo the engine's own CLI advice (SQM / fq_codel / CAKE / shaper /
line rate / OpenWrt / pfSense all present) — UI and CLI cannot disagree.

### 6. UI / a11y / icons — PASS
- All referenced Theme tokens exist (`ThemeTokens.swift`: gradeA–F ramp, surface, separator,
  accent, primaryText/secondaryText, Spacing xs/sm/md/lg, Radius card/control).
- A11y: container `.contain` + label "Bufferbloat story" + spoken value ("Grade X, N ms…");
  decorative grade letter and icons `.accessibilityHidden`; activity rows use `.ignore` +
  title label + impact value; delta text carries a unit-bearing spoken label.
- Icons: `video.fill`, `gamecontroller`, `film`, `lightbulb.fill` confirmed PRESENT in the
  system SF Symbol catalog AND present in the compiled binary (`strings` check ×4).

### 7. Anti-synthesis scan — CLEAN
No TODO/FIXME/XFAIL/fatalError stubs. Self-check is real executable logic (executed above),
not decorative. No invented APIs: `MetricExtractor.latestBloatGrade(in:)` exists
(DashboardCardsView.swift:198) returning `(letter:, deltaMs:)` exactly as the view's
integration notes consume it; `MetricExtractor.validGrades = {A+,A,B,C,D,F}` ≡
`Band(letter:)` accepted set — seam compatible.

## Findings

| # | Severity | Finding |
|---|----------|---------|
| 1 | MINOR (non-blocking) | B-band cause says "adding up to 60 ms" but band B is `< 60`, i.e. 30–59.9 ms. Cosmetic overstatement inside otherwise-correct copy; suggest "roughly 30–60 ms" phrasing like the gaming row already uses. |
| 2 | NOTE (non-blocking) | Parity drift guard (`verifiedOnce` assert) fires only in DEBUG builds; release builds trust the static ladder. Acceptable — ladder verified identical today and the model is pure. |

## Blockers

None. **READY-TO-MERGE.**

## Verifier tooling transparency
Two mid-audit tool faults were verifier-side and fixed, not builder defects: a harness
`String(format:)` `%s` misuse (segfaulted before printing — replaced with interpolation) and a
literal scanner that initially swept comment text (fixed by comment stripping). Final results
above are from the corrected tooling.
